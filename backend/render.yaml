services:
  - type: web
    name: openlexer-backend
    env: docker
    region: oregon
    plan: free
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000 
envVarGroups: []
